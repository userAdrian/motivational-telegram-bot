# Main Spring Boot application properties

spring.profiles.active=test
spring.application.name=motivational-telegram-bot
server.port=8080

# Message resource bundle base name
spring.messages.basename=messages