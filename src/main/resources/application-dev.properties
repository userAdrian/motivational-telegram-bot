# Spring Boot application properties for development environment

# -------------------- REDIS CONFIGURATION --------------------
# Redis server connection details for caching
spring.data.redis.host=redis-18995.c135.eu-central-1-1.ec2.redns.redis-cloud.com
spring.data.redis.port=18995
spring.data.redis.username=username
spring.data.redis.password=password

# -------------------- DATABASE CONFIGURATION --------------------
# H2 in-memory database configuration for development and testing
spring.datasource.url=jdbc:h2:mem:myDB
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
spring.jpa.hibernate.ddl-auto=create

# -------------------- REDISSON CONFIGURATION --------------------
# Redisson configuration file for distributed locks
spring.redis.redisson.file=classpath:config/dev/redisson-dev.yaml

# -------------------- IMPORT ADDITIONAL CONFIGURATION --------------------
# Import page and API configuration files
spring.config.import=classpath:config/dev/api-dev.properties,classpath:config/dev/pages-dev.properties

# -------------------- LOGGING CONFIGURATION --------------------
# Maximum request/response body length to log (in bytes)
motivational.telegram.bot.configuration.log.max-request-length=2000000000
motivational.telegram.bot.configuration.log.max-response-length=200000000

# -------------------- PHRASE SCHEDULER CONFIGURATION --------------------
# Times (HH:mm:ss) when phrases should be sent (comma-separated)
motivational.telegram.bot.configuration.phrase.sending-times=22:27:00,22:17:30,21:57:59
# Time zone for phrase scheduling
motivational.telegram.bot.configuration.phrase.time-zone=Europe/Rome
# Number of seconds to add to the delay before processing the next user after each integration error
# (e.g. if delay=0 -> 1st sleepTime=30s, 2nd 60s, etc.)
motivational.telegram.bot.configuration.phrase.scheduler-delay-increment=30
# Maximum duration (in seconds) allowed for a single delay interval before proceeding
motivational.telegram.bot.configuration.phrase.scheduler-max-delay=600
# Maximum time (seconds) to lease the lock
motivational.telegram.bot.configuration.phrase.scheduler-lock-lease-time=600

# -------------------- COOLDOWN CONFIGURATION --------------------
# Cooldown durations for commands (ISO-8601 format, e.g. PT6H = 6 hours)
motivational.telegram.bot.configuration.cooldown.cooldown-duration-map.RANDOM_PHRASE=PT6H

# -------------------- COMMAND AUTHORIZATION CONFIGURATION --------------------
# Command authorization map (format: command-text=UserRoleSet)
motivational.telegram.bot.configuration.command.auth-roles-map[/loadFilePhrases]=ALL
# Command cooldown skipping roles
motivational.telegram.bot.configuration.command.skip-cooldown-roles=ALL